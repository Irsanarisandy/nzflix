export default {
    container: {
        backgroundColor: 'black',
        color: 'white',
        height: window.innerHeight
    },
    video: {
        height: window.innerWidth / 2,
        width: window.innerWidth
    }
};
